# get-out-of-bed
The ONE alarm I need

Dev Notes:
- The alarm module is using platform.io using VS Code
- The alarm switch module is using Arduino IDE since I couldn't figure out how to make it work with platform.io
